import { View } from "react-native";

export const LoadingSpinner = () => {
  return (
    <View className="w-6 h-6 border-[2.5px] border-neutral-200 border-t-ascent rounded-full animate-spin" />
  );
};
